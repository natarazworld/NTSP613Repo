CREATE TABLE "SYSTEM"."POLITICALPARTY" 
   (	"PARTYID" NUMBER(19,0) NOT NULL ENABLE, 
	"FLAGCOLORS" VARCHAR2(20 CHAR), 
	"PARTYNAME" VARCHAR2(20 CHAR), 
	"PARTYSYMBOL" VARCHAR2(20 CHAR), 
	 PRIMARY KEY ("PARTYID"));