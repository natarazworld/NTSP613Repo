==== DB table in oralce ===================
CREATE TABLE "SYSTEM"."DTX_BANK_ACCOUNT" 
   (	"ACCNO" NUMBER(12,0) NOT NULL ENABLE, 
	"HOLDERNAME" VARCHAR2(20 BYTE), 
	"BALANCE" FLOAT(126), 
	 CONSTRAINT "DTX_BANK_ACCOUNT_PK" PRIMARY KEY ("ACCNO"))
	 
	 ==== DB table in oralce ===================
CREATE TABLE "DTX_BANK_ACCOUNT" 
   (	"ACCNO" INT(10) NOT NULL, 
	"HOLDERNAME" VARCHAR(20), 
	"BALANCE" FLOAT(126), 
	 CONSTRAINT "DTX_BANK_ACCOUNT_PK" PRIMARY KEY ("ACCNO"));