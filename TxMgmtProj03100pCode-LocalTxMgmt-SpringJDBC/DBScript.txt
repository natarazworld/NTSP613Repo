CREATE TABLE "SYSTEM"."SPRING_TX_BANK" 
   (	"ACCNO" NUMBER(10,0) NOT NULL ENABLE, 
	"HOLDERNAME" VARCHAR2(20 BYTE), 
	"BALANCE" FLOAT(126), 
	 CONSTRAINT "SPRING_TX_BANK_PK" PRIMARY KEY ("ACCNO"));